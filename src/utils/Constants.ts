export const PHYSICS = {
  FORCE_SCALE: 1000,
  TORQUE_SCALE: 1000,
  GRAVITY: 0.5,
} as const;

export const COLORS = {
  BACKGROUND: "#000",
  BOX: "#0066cc",
  PARTICLE: "#ff6b6b",
  BOUNDARY: "#333",
} as const;

export const CANVAS = {
  DEFAULT_WIDTH: 800,
  DEFAULT_HEIGHT: 600,
} as const;
